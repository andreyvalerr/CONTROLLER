## 📊 Общая информация
**CONTROLLER** - это комплексная система автоматического контроля температуры для криптокотлов на базе Raspberry Pi. Проект предназначен для управления системой охлаждения криптомайнеров путем автоматического включения/выключения клапанов охлаждения на основе данных о температуре жидкости от асика Whatsminer M64_VL40.

## 🏗️ Архитектура проекта

### Модульная структура:

1. **`core/`** - Центральное ядро системы
2. **`valve_control/`** - Основной модуль управления клапанами  
3. **`get_temperature/`** - Модуль получения температуры от асика
4. **`gui_interface/`** - Графический интерфейс для сенсорного дисплея
5. **`Documentation/`** - Техническая документация

### Схема взаимодействия модулей:
```
┌─────────────────┐    ┌──────────────────┐    ┌─────────────────┐
│  GUI Interface  │◄───┤ SharedSystemState├───►│ ValveController │
│   (Kivy GUI)    │    │   (Observer)     │    │  (Main Logic)   │
└─────────────────┘    └──────────────────┘    └─────────────────┘
                                │                         │
                                ▼                         ▼
                       ┌─────────────────┐    ┌─────────────────┐
                       │ TemperatureAPI  │    │ RelayController │
                       │  (Whatsminer)   │    │   (GPIO)        │
                       └─────────────────┘    └─────────────────┘
```

## 📋 Ключевые компоненты

### 1. Центральное ядро (`core/`)
- **`shared_state.py`** (195 строк) - Потокобезопасное хранилище состояния системы
- **`data_models.py`** - Модели данных (TemperatureData, SystemStatus, SystemSettings)
- **`__init__.py`** - Инициализация модуля

**Функции ядра:**
- Централизованное управление состоянием системы
- Паттерн Observer для уведомления компонентов
- Потокобезопасные операции с блокировками
- Валидация данных и настроек

### 2. Система управления клапанами (`valve_control/`)
- **`valve_controller.py`** (456 строк) - Главный контроллер системы
- **`relay_controller.py`** - Низкоуровневое управление GPIO реле
- **`temperature_regulator.py`** - Логика регулирования с гистерезисом
- **`config.py`** (162 строки) - Система конфигурации с поддержкой переменных окружения
- **`main.py`** - Альтернативная точка входа без GUI
- **`manual_control.py`** - Ручное управление клапанами
- **`test_system.py`** - Системные тесты
- **`integration_test.py`** - Интеграционные тесты

**Алгоритм управления:**
- Гистерезисное управление для предотвращения частых переключений
- Многоуровневая система безопасности
- Автоматическое и ручное управление
- Защита от перегрева и аварийные остановки

### 3. Модуль получения температуры (`get_temperature/`)
- **`simple_api.py`** (305 строк) - Унифицированный API для получения температуры 
- **`simple_monitor.py`** - Мониторинг температуры с автопереподключением
- **`whatsminer_interface.py`** - Специфичный интерфейс с Whatsminer M64_VL40
- **`whatsminer_transport.py`** - TCP транспорт для связи с асиком
- **`monitor_class.py`** - Классовый интерфейс мониторинга
- **`api_interface.py`** - Дополнительные API интерфейсы
- **`start_monitoring.py`** - Автономный запуск мониторинга

**Возможности модуля:**
- Поддержка различных протоколов связи с майнерами
- Автоматическое переподключение при сбоях
- Статистика подключений и ошибок
- Кэширование данных температуры

### 4. Графический интерфейс (`gui_interface/`)
- **`main_gui.py`** (419 строк) - Современный GUI на базе Kivy
- **`assets/`** - Графические ресурсы
- **`components/`** - Компоненты интерфейса
- **`styles/`** - Стили оформления

**Особенности GUI:**
- Карточная структура интерфейса
- Real-time обновление данных
- Сенсорное управление для Raspberry Pi
- Настройка температурных порогов
- Визуализация состояния системы

### 5. Системные файлы и сервисы
- **`start_system_with_gui.py`** (165 строк) - Главная точка входа с GUI
- **`crypto-boiler.service`** - Systemd сервис для автозапуска
- **`install_service.sh`** - Скрипт установки сервиса
- **`system.log`** - Файл логов системы

## ⚙️ Принцип работы

### Алгоритм управления:
1. **Инициализация системы** - загрузка конфигурации из переменных окружения
2. **Запуск мониторинга** - подключение к асику и получение данных о температуре
3. **Обработка данных** - обновление SharedSystemState и уведомление подписчиков
4. **Анализ температуры** - сравнение с пороговыми значениями
5. **Управление клапаном** - включение/выключение охлаждения через GPIO реле
6. **Отображение в GUI** - обновление интерфейса с актуальными данными

### Гистерезисное управление:
- **Включение охлаждения**: при температуре ≥ `MAX_TEMP=52.0°C`
- **Выключение охлаждения**: при температуре ≤ `MIN_TEMP=51.9°C`
- **Гистерезис**: `0.1°C` для предотвращения частых переключений

### Многоуровневая защита:
- **Критическая температура**: `65°C` - предупреждения и усиленное охлаждение
- **Аварийная температура**: `70°C` - аварийная остановка системы
- **Таймаут соединения**: `300 секунд` - аварийная остановка при потере связи
- **Ограничение переключений**: максимум `6600 переключений/час`

### Технические особенности:
- Потокобезопасная архитектура с использованием threading.RLock()
- Поддержка переменных окружения для гибкой конфигурации
- Graceful shutdown при получении сигналов SIGINT/SIGTERM
- Автоматическое восстановление соединений
- Подробное логирование всех операций

## 🚀 Способы запуска

### 1. Полная система с GUI интерфейсом
```bash
cd CONTROLLER
python3 start_system_with_gui.py
```
**Функции:**
- Запуск мониторинга температуры асика
- Активация GUI интерфейса с сенсорным управлением
- Real-time отображение данных
- Возможность настройки параметров через интерфейс

### 2. Системный сервис (рекомендуется для production)
```bash
sudo ./install_service.sh
sudo systemctl start crypto-boiler
sudo systemctl enable crypto-boiler  # Автозапуск при загрузке
```
**Преимущества:**
- Автоматический запуск при загрузке системы
- Автоматический перезапуск при сбоях
- Работа в фоновом режиме
- Интеграция с systemd для управления

### 3. Только управление клапанами (без GUI)
```bash
cd CONTROLLER/valve_control
python3 main.py
```
**Применение:**
- Headless серверы без дисплея
- Минимальное потребление ресурсов
- Интеграция в другие системы

### 4. Фоновый режим
```bash
cd CONTROLLER
nohup python3 start_system_with_gui.py > system.log 2>&1 &
```

### 5. Тестовый режим
```bash
cd CONTROLLER/valve_control
python3 test_system.py  # Тестирование реле
python3 integration_test.py  # Интеграционные тесты
```

## 🔧 Система конфигурации

### Иерархия конфигурации:
1. **Значения по умолчанию** в `valve_control/config.py`
2. **Переменные окружения** для runtime настройки
3. **Конфигурация сервиса** в `crypto-boiler.service`

### Основные настройки:

#### Сетевые параметры:
- **`ASIC_IP`**: `192.168.0.127` - IP адрес майнера Whatsminer
- **`UPDATE_INTERVAL`**: `1.0` - интервал обновления температуры (сек)

#### GPIO и реле:
- **`RELAY_PIN`**: `17` - GPIO пин реле (BCM нумерация)
- **`GPIO_MODE`**: `BCM` - режим нумерации GPIO
- **`GPIO_CLEANUP`**: `true` - очистка GPIO при выходе

#### Температурные пороги:
- **`MAX_TEMP`**: `52.0°C` - температура включения охлаждения
- **`MIN_TEMP`**: `51.9°C` - температура выключения охлаждения  
- **`CRITICAL_TEMP`**: `65.0°C` - критическая температура
- **`EMERGENCY_TEMP`**: `70.0°C` - аварийная температура
- **`TEMP_HYSTERESIS`**: `0.1°C` - гистерезис температуры

#### Безопасность и таймауты:
- **`TEMP_TIMEOUT`**: `10.0` - таймаут получения температуры (сек)
- **`EMERGENCY_TIMEOUT`**: `300.0` - аварийный таймаут при потере связи (сек)
- **`MAX_SWITCHES_HOUR`**: `6600` - максимум переключений в час

#### Логирование:
- **`LOG_LEVEL`**: `INFO` - уровень логирования
- **`LOG_FILE`**: `system.log` - файл логов
- **`CONSOLE_LOG`**: `true` - вывод в консоль

## 💻 GUI интерфейс

### Компоненты интерфейса:
- **TemperatureCard** - карточка отображения температуры с цветовой индикацией
- **StatusCard** - карточка состояния системы (клапан, режим, время обновления)
- **ControlPanel** - панель управления с настройками температуры
- **TemperatureSettingsPopup** - всплывающее окно настройки температурных порогов

### Функции GUI:
- **Real-time мониторинг** - автоматическое обновление данных каждую секунду
- **Цветовая индикация** - красный/зеленый/голубой для разных состояний температуры
- **Сенсорное управление** - оптимизация для сенсорных дисплеев
- **Настройка параметров** - изменение температурных порогов через слайдеры
- **Уведомления об ошибках** - отображение проблем соединения

## 📊 Статистика проекта
- **Общий объем кода**: ~1200+ строк Python
- **Основных модулей**: 4 (core, valve_control, get_temperature, gui_interface)
- **Python файлов**: 20+ файлов
- **Тестовых файлов**: 3 (test_system.py, integration_test.py, test_simple.py)
- **Конфигурационных файлов**: 4
- **Документация**: README.md, SOLUTION.md, test_report.md

## 🔒 Безопасность и надежность

### Защитные механизмы:
- **Аварийное отключение** при критических температурах (65°C/70°C)
- **Timeout защита** - остановка при потере связи с асиком более 5 минут
- **Защита от частых переключений** - ограничение количества включений/выключений
- **GPIO cleanup** - правильная очистка ресурсов при завершении
- **Signal handling** - корректная обработка сигналов SIGINT/SIGTERM

### Мониторинг и диагностика:
- **Подробное логирование** всех операций в `system.log`
- **Статистика работы** - количество успешных/неуспешных запросов
- **Отслеживание ошибок** - сохранение информации о сбоях
- **Автоматический перезапуск** сервиса при критических ошибках

### Отказоустойчивость:
- **Автоматическое переподключение** к асику при сбоях сети
- **Graceful degradation** - работа в режиме эмуляции при недоступности асика
- **Потокобезопасность** - использование блокировок для многопоточного доступа
- **Валидация данных** - проверка корректности всех входных параметров

## 🎯 Назначение и применение

### Основное применение:
Система создана для **промышленного использования в криптофермах** для автоматического поддержания оптимальной температуры охлаждающей жидкости, обеспечивая:

- **Стабильную работу** майнингового оборудования 24/7
- **Предотвращение перегрева** и связанных с ним поломок
- **Минимизацию человеческого участия** в процессе охлаждения
- **Оптимизацию энергопотребления** системы охлаждения
- **Продление срока службы** оборудования

### Технические преимущества:
- **Модульная архитектура** - легкость модификации и расширения
- **Профессиональный GUI** - удобство эксплуатации и мониторинга  
- **Промышленная надежность** - защита от всех типов сбоев
- **Гибкая конфигурация** - адаптация под различные условия эксплуатации
- **Полная автоматизация** - работа без постоянного присутствия оператора

Проект демонстрирует профессиональный подход к разработке промышленных IoT-систем с учетом всех требований к надежности, безопасности и удобству эксплуатации в условиях криптофермы.
